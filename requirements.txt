# AskMiro Lead Intelligence OS — Python dependencies
# Core
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0          # faster HTML parser for BeautifulSoup

# AI providers (install the one you use)
openai>=1.12.0        # if AI_PROVIDER=openai
anthropic>=0.18.0     # if AI_PROVIDER=anthropic

# Data / export
tabulate>=0.9.0       # optional: richer table formatting
python-dateutil>=2.8.0
python-multipart>=0.0.9  # FastAPI file uploads
httpx>=0.27.0            # async HTTP client

# Dev / testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Web API
fastapi>=0.110.0
uvicorn>=0.27.0
apscheduler>=3.10.0  # background scheduler for CRM auto-push

# PostgreSQL
psycopg2-binary>=2.9.9
rapidfuzz>=3.0.0     # fuzzy name matching for entity resolution

# Auth
authlib>=1.3.0       # Google OAuth
itsdangerous>=2.1.0  # session signing
starlette[full]>=0.36.0  # session middleware for OAuth state

# Email deliverability
dnspython>=2.4.0     # MX record validation for email guard
