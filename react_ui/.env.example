# ── GAS Backend (operational data: quotes, finance, cleaners) ──
# Set both of these in Railway dashboard → Variables
VITE_GAS_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
VITE_GAS_TOKEN=YOUR_TOKEN
